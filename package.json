{
  "name": "shanique-ehr-edi",
  "version": "1.0.0",
  "description": "EHR system with EDI X12 837P integration for Availity clearinghouse",
  "main": "src/api/server.js",
  "scripts": {
    "start": "NODE_ENV=production node src/api/server.js",
    "dev": "NODE_ENV=development nodemon src/api/server.js",
    "test": "jest --testPathPattern=tests/ --verbose",
    "test:edi": "jest tests/edi.test.js --verbose"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "uuid": "^9.0.0",
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
